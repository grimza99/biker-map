export type BikerPreview = {
  nickname: string;
  bikeBrand: string;
  bikeModel: string;
  distance: string;
  proficiency: string;
};

export type TLocationCoordinate = {
  lat: number;
  lng: number;
};
